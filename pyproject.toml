[project]
name = "randomizer"
version = "0.1.0"
description = "Randomizing API built with FastAPI"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.135.3",
]

[dependency-groups]
dev = [
    "ruff>=0.15.9",
]
